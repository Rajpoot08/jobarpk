<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Job Search Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="style.css">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <style>
        /* Custom CSS for Job Finder - New Hero Section Only (from previous step, kept for context) */

:root {
    /* Define your color variables here if not already in your style.css */
    --new-hero-bg-color: #2e3b4a; /* A dark base color for the background */
    --new-hero-heading-white: #ffffff;
    --new-hero-heading-yellow: #ffc107; /* Bootstrap's yellow */
    --new-hero-heading-red: #dc3545; /* Bootstrap's red for "love" */

    /* Ensure these are also defined for other sections if needed */
    --indeed-blue: #2557a7;
    --indeed-dark-grey: #4b4b4b;

    /* NEW: Search Page Colors */
    --search-page-bg: #212529; /* Light black / Very dark grey */
    --search-page-input-bg: #495057; /* Slightly lighter grey for input background */
    --search-page-text-color: #dee2e6; /* Light text color */
}

body {
    font-family: 'Roboto', sans-serif;
    background-color: #f3f2f1; /* Adjust if your overall body background changes */
}

/* Existing Navbar and Hero Section Styles ... (keep your existing styles here) */
/* ... (e.g., .indeed-logo, .custom-nav-link, .indeed-btn-notify, .new-hero-section, etc.) */


/* --- Search Results Page Specific Styles --- */
.search-page-body {
    background-color: var(--search-page-bg); /* Light black background */
    display: flex; /* Use flexbox to center content */
    flex-direction: column; /* Stack header and main content vertically */
    min-height: 100vh; /* Ensure it takes full viewport height */
    overflow-x: hidden; /* Prevent horizontal scroll */
}

.close-button-container {
    position: absolute;
    top: 20px;
    left: 20px;
    z-index: 1050; /* Above other content */
}

.btn-close-custom {
    color: var(--search-page-text-color); /* White/light grey X icon */
    font-size: 2rem; /* Large X icon */
    opacity: 0.8; /* Slightly transparent */
    transition: opacity 0.3s ease, color 0.3s ease;
    text-decoration: none; /* Remove underline */
}
.btn-close-custom:hover {
    color: white; /* Brighter white on hover */
    opacity: 1;
}

.search-page-headline {
    font-size: 2.5rem;
    font-weight: 700;
    color: var(--new-hero-heading-white); /* White for the headline */
    max-width: 700px;
}

.search-page-form {
    max-width: 700px; /* Max width for the form */
    width: 100%;
}

.search-page-input-group {
    flex-grow: 1;
    border: 1px solid var(--search-page-input-bg); /* Darker border */
    border-radius: 0.5rem; /* Slightly rounded corners */
    overflow: hidden;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3); /* Subtle shadow */
}
.search-page-input-group:focus-within {
    border-color: var(--indeed-blue); /* Blue border on focus */
    box-shadow: 0 0 0 0.25rem rgba(37, 87, 167, 0.25); /* Bootstrap-like focus glow */
}

.search-page-form-control {
    background-color: var(--search-page-input-bg); /* Dark background for input */
    border: none;
    box-shadow: none;
    color: var(--search-page-text-color); /* Light text color */
    padding: 1rem 1.25rem;
    font-size: 1.1rem;
    height: auto;
}
.search-page-form-control::placeholder {
    color: rgba(255, 255, 255, 0.6); /* Lighter placeholder text */
}

.search-page-search-btn {
    background-color: var(--indeed-blue) !important;
    border-color: var(--indeed-blue) !important;
    font-weight: 700;
    padding: 1rem 2rem;
    border-radius: 0.5rem; /* Rounded corners */
    font-size: 1.1rem;
    min-width: 150px;
}
.search-page-search-btn:hover {
    background-color: #1f478a !important; /* Darker blue on hover */
    border-color: #1f478a !important;
}

.search-results-placeholder {
    font-size: 1.1rem;
    text-align: center;
    color: rgba(255, 255, 255, 0.7); /* Slightly transparent white */
}

/* Responsive adjustments for the search page */
@media (max-width: 767.98px) {
    .close-button-container {
        top: 15px;
        left: 15px;
    }
    .btn-close-custom {
        font-size: 1.8rem;
    }
    .search-page-headline {
        font-size: 2rem;
    }
    .search-page-form {
        flex-direction: column;
        align-items: center;
    }
    .search-page-input-group {
        width: 100%;
        margin-right: 0 !important;
        margin-bottom: 15px !important;
    }
    .search-page-search-btn {
        width: 100%;
        min-width: unset;
    }
}

@media (max-width: 480px) {
    .search-page-headline {
        font-size: 1.8rem;
    }
}
    </style>
</head>
<body class="search-page-body">

    <div class="close-button-container">
        <a href="/" class="btn btn-close-custom" aria-label="Close">
            <i class="bi bi-x-lg"></i> </a>
    </div>

    <div class="container d-flex flex-column align-items-center justify-content-center min-vh-100 py-5">
        <h1 class="text-white mb-4 search-page-headline">Find Your Next Opportunity</h1>

        <form class="d-flex justify-content-center flex-wrap flex-md-nowrap search-page-form" id="searchPageForm">
            <div class="input-group search-page-input-group me-md-3 mb-3 mb-md-0">
                <input type="text" class="form-control search-page-form-control" placeholder="Job title, keywords, or company" aria-label="Job title, keywords, or company">
            </div>
            <button type="submit" class="btn btn-primary search-page-search-btn">Search</button>
        </form>

        <div class="search-results-placeholder text-white-50 mt-5">
            <p>Search is not working correctely! We are working on it</p>
            <p>Please use it later</p>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <script>
        // Simple redirection for the search button on this page (if needed)
        document.addEventListener('DOMContentLoaded', () => {
            const searchPageForm = document.getElementById('searchPageForm');
            if (searchPageForm) {
                searchPageForm.addEventListener('submit', (event) => {
                    event.preventDefault(); // Prevent default form submission
                    const searchTerm = searchPageForm.querySelector('input').value;
                    alert(`Searching for: ${searchTerm}. (This would typically fetch results)`);
                    // In a real app, you'd fetch data here and populate the .search-results-placeholder div
                });
            }
        });
    </script>
</body>
</html>